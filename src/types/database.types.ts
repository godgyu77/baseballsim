/**
 * Supabase Database 타입 정의
 * 
 * Supabase CLI를 사용하여 타입을 생성하려면:
 * npx supabase gen types typescript --project-id <your-project-id> > src/types/database.types.ts
 * 
 * 또는 Supabase Dashboard에서 직접 생성한 타입을 여기에 붙여넣으세요.
 */

// TODO: Supabase에서 생성한 Database 타입을 여기에 추가하세요
// 현재는 임시로 any 타입을 사용합니다
export type Database = any;

